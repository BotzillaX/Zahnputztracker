#!/bin/sh
# Wortlistenpruefung vor jedem Commit (Entscheidung 22).
if [ -x ".venv/Scripts/python.exe" ]; then
  PY=".venv/Scripts/python.exe"
else
  PY="python"
fi
exec "$PY" scripts/tarnung.py --staged
